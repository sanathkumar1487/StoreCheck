<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.euromonitor.storecheck">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

        <activity
            android:name=".app.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.AndroidDatabaseManager"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity android:name=".app.StoreCheckAddBrandActivity" />
        <activity android:name=".app.StoreCheckAddProductDetailsActivity" />
        <activity android:name=".app.StoreCheckImportActivity" />
        <activity android:name=".app.StoreCheckExportActivity" />
        <activity android:name=".app.StoreCheckDetailsActivity" />
        <activity
            android:name=".app.StoreCheckOutletDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".app.OutletDetailsActivity"/>
        <activity android:name=".app.AddOutletActivity"/>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="4b345bd310a45cfb27c568757996a5392a62d50c" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
